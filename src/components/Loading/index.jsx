import React from "react";
import { LoadingContainer, LoadingIcon } from "./style";

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingIcon />
    </LoadingContainer>
  );
};

export default Loading;
